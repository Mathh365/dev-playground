namespace FuncoesDoMenu
{
    public static class Func03
    {
        public static void Exec()
        {
            // Console.Write("Quantas notas serao inseridas para o calculo?\nQuantidade de notas: ");
            // int quantidadeDeNotas = Convert.ToInt16(Console.ReadLine());

            // double mediaFinal = 0;
            // double[] notas = new double[quantidadeDeNotas];
            // double[] pesos = new double[quantidadeDeNotas];
            // double[] mediaNaoFinal = new double[quantidadeDeNotas];

            // for (int i = 0; i < quantidadeDeNotas; i++)
            // {
            //     Console.Write($"Digite a nota {i + 1}\nNota: ");
            //     notas[i] = Convert.ToDouble(Console.ReadLine());

            //     Console.Write("E qual é o peso desta nota?\nPeso: ");
            //     pesos[i] = Convert.ToDouble(Console.ReadLine());

            //     mediaNaoFinal[i] = notas[i] * pesos[i];
            //     mediaFinal += mediaNaoFinal[i];
            //     Console.WriteLine($"\nSua nota desta avaliaçao é {mediaNaoFinal}");
            // }

            // Console.WriteLine($"E para finalizar, sua media final é {mediaFinal}");
            MyUtil.Retorno();
        }
    }
}